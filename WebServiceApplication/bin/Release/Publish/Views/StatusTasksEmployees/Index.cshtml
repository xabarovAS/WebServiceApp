@model IEnumerable<WebServiceApplication.Models.StatusTasksEmployee>

@{
    ViewBag.Title = "Index";
}

<div class="text-center">
    <p>
    <p>
        <h2> Список статусов</h2>
    </p>
    </p>
</div>

<p>
   @* <a class="btn btn-outline-primary" href="/StatusTasksEmployees/Create" role="button">Добавить</a>*@
</p>
<table class="table">
    <tr>
        <th>
            Сотрудник
        </th>
        <th>
            Задача
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatusTask)
        </th>
        <th></th>
    </tr>


    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employeе.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TasksEmployee.FullName)
            </td>
            <td>
                
                @if (item.StatusTask == WebServiceApplication.Models.StatusTask.Done)
                {
                    <text>Выполнено</text>
                }
                else
                {
                    <text>Не выполнено</text>                   
                }
            </td>

            <td>                
                @Html.ActionLink("Удалить", "Delete", new { id = item.ID }, new { @class = "btn btn-outline-danger" })
            </td>
        </tr>
    }


</table>
